const config = {
  plugins: {
    // Tailwind CSS v4 automatically includes its PostCSS plugin.
    // Explicitly adding it here can cause conflicts or is redundant.
    // Remove the '@tailwindcss/postcss': {} line.
  },
};
export default config;